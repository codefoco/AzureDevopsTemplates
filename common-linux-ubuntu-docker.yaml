
steps:
  - task: DockerInstaller@0
    inputs:
      dockerVersion: '29.7.2'
      releaseType: 'stable'
      
  - bash: |
          curl -L -o ~/.docker/cli-plugins/docker-buildx --create-dirs https://github.com/docker/buildx/releases/download/v0.36.0/buildx-v0.36.0.linux-amd64
          chmod a+x ~/.docker/cli-plugins/docker-buildx
          docker run --privileged --rm tonistiigi/binfmt:qemu-v10.2.3-68 --install all
          ~/.docker/cli-plugins/docker-buildx create --use
          ~/.docker/cli-plugins/docker-buildx inspect --bootstrap
          sudo echo '{ "experimental": true }' | sudo tee /etc/docker/daemon.json
    displayName: Prepare Docker buildx
