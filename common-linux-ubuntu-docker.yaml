
steps:
  - task: DockerInstaller@0
    inputs:
      dockerVersion: '29.6.2'
      releaseType: 'stable'
      
  - bash: |
          curl -L -o ~/.docker/cli-plugins/docker-buildx --create-dirs https://github.com/docker/buildx/releases/download/v0.35.0/buildx-v0.35.0.linux-amd64
          chmod a+x ~/.docker/cli-plugins/docker-buildx
          docker run --privileged --rm tonistiigi/binfmt:qemu-v10.2.3-67 --install all
          ~/.docker/cli-plugins/docker-buildx create --use
          ~/.docker/cli-plugins/docker-buildx inspect --bootstrap
          sudo echo '{ "experimental": true }' | sudo tee /etc/docker/daemon.json
    displayName: Prepare Docker buildx
